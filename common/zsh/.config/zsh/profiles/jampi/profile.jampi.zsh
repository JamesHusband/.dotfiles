# ==============================================================================
# JamPI - Raspberry Pi Home Server
# ==============================================================================


[[ $HOST != "jampi" ]] && return

export TERM=xterm-256color

echo "Is JamPI! Setting up SSH configuration for JamPI..."
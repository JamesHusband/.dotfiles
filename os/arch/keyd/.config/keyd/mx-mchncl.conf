[ids]
046d:b366

[main]
# Ghost fix: / produces repeated keypresses — disable it
/ = noop
# Backslash key acts as / replacement
backslash = /

[shift]
# shift+/ still produces ?
/ = /
# shift+backslash produces | (shift+backslash) — verify with `sudo keyd monitor`
# on GB layout with US physical keyboard this may need adjustment
backslash = backslash

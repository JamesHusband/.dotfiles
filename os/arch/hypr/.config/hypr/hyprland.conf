# Nvidia
env = LIBVA_DRIVER_NAME,nvidia

# Gnome keyring detection
env = GNOME_DESKTOP_SESSION_ID,1
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
cursor {
    no_hardware_cursors = true
}

# Monitor
monitor = HDMI-A-1,preferred,auto,1

# Autostart
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = swaybg -o HDMI-A-1 -i /home/jam/.jam-sys/assets/wallpaper.png -m fill
exec-once = waybar
exec-once = mako
exec-once = hypridle
exec-once = nm-applet --indicator
exec-once = blueman-applet

# Input
input {
    kb_layout = gb
    follow_mouse = 1
    touchpad {
        natural_scroll = false
    }
}

# General
general {
    gaps_in = 4
    gaps_out = 8
    border_size = 2
    col.active_border = rgba(cba6f7ff) rgba(89b4faff) 45deg
    col.inactive_border = rgba(313244ff)
    layout = dwindle
}

# Decoration
decoration {
    rounding = 8
    blur {
        enabled = false
        size = 4
        passes = 2
    }
}

# Animations
animations {
    enabled = false
    bezier = ease, 0.4, 0, 0.2, 1
    animation = windows, 1, 3, ease, slide
    animation = fade, 1, 3, ease
    animation = workspaces, 1, 3, ease
}

# Layout
dwindle {
    pseudotile = true
    preserve_split = true
}

# Keybinds
$mod = SUPER

bind = $mod, Return, exec, ghostty
bind = $mod SHIFT, Q, killactive
bind = $mod, D, exec, wofi --show drun
bind = $mod, F, fullscreen
bind = $mod SHIFT, Space, togglefloating
bind = $mod, P, pseudo
bind = $mod, V, togglesplit

# Focus
bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d
bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

# Move windows
bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, L, movewindow, r
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, J, movewindow, d

# Workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Mouse
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Volume
bind = $mod, XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = $mod, XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = $mod, XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

# Lock
bind = $mod, Escape, exec, hyprlock
